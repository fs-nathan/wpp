const rootPath = "/calendar";
export const Routes = {
  WEEKLY: rootPath + "/weekly",
  PROJECT: rootPath + "/project",
  ALARM_RECENTLY: "/calendar-alarm/recently",
  PARENT: rootPath
};

export const LinkRoutes = {
  WEEKLY: "/calendar-weekly-detail",
  PROJECT: "/calendar-project-detail",
}