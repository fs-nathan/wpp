const rootPath = "/tasks/overview";
export const Routes = {
  OVERVIEW: rootPath,
  DUE: rootPath + "/due",
  EXPIRED: rootPath + "/expired",
  MISSION: rootPath + "/assign/:typeAssign",
  ROLE: rootPath + "/role/:roleId",
};
