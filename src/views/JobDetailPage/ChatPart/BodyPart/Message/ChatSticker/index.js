import './styles.scss';

import React from 'react';
import PropTypes from 'prop-types';

const ChatSticker = (props) => {

  return (
    <div className="ChatSticker">
    </div>
  );
}

ChatSticker.propTypes = {

};

export default ChatSticker;
