import React from 'react';
import './styles.scss';


const RemindMessage = (props) => {

  return (
    <div className="RemindMessage">
      <div className="RemindMessage--child">
        {'text'}
      </div>
    </div>
  );
}

RemindMessage.propTypes = {

};

export default RemindMessage;
