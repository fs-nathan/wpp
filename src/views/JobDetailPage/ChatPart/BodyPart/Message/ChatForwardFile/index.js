import './styles.scss';

import React from 'react';
import PropTypes from 'prop-types';

const ChatForwardFile = (props) => {

  return (
    <div className="ChatForwardFile">
    </div>
  );
}

ChatForwardFile.propTypes = {

};

export default ChatForwardFile;
