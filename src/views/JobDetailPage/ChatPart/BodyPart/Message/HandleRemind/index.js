import './styles.scss';

import React from 'react';
import PropTypes from 'prop-types';

const HandleRemind = (props) => {

  return (
    <div className="HandleRemind">
    </div>
  );
}

HandleRemind.propTypes = {

};

export default HandleRemind;
